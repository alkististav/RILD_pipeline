#!/usr/bin/env python

from setuptools import setup


setup(name='RILD_pipeline',
      version='0.1',
      description='CT data formatting and processing pipeline run from main CLI',
      author='UCL RSDG',
      author_email='',
      url='',
      packages=['RILD_pipeline'],
      )
